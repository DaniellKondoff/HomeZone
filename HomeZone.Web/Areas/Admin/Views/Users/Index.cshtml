@model AdminUsersListingViewModel
@{
    ViewData["Title"] = "User Administration";
}

<h2>@ViewData["Title"]</h2>

<table class="table table-bordered table-hover">
    <thead>
        <tr>
            <th>
                UserName
            </th>
            <th>
                E-mail
            </th>
            <th>
            </th>
            <th>
            </th>
        </tr>
    </thead>
    <tbody>
        @foreach (var user in Model.Users)
        {
            <tr>
                <td>
                    @user.UserName
                </td>
                <td>
                    @user.Email
                </td>
                <td>
                    <form asp-controller="Users" asp-action="AddToRole" method="post">
                        <div class="row">
                            <div class="col-md-9">
                                <input type="hidden" name="UserId" value="@user.Id" />
                                <select class="form-control" name="Role" asp-items="Model.Roles"></select>
                            </div>
                            <div class="col-md-3">
                                <input class="btn btn-sm btn-primary" type="submit" value="Add To Role" />
                            </div>
                        </div>
                    </form>
                </td>
                <td>
                    <form asp-controller="Users" asp-action="RemoveRole" method="post">
                        <div class="row">
                            <div class="col-md-3">
                                <input type="hidden" name="UserId" value="@user.Id" />
                                <input class="btn btn-sm btn-primary" type="submit" value="Remove Role" />
                            </div>
                        </div>
                    </form>
                </td>
            </tr>
        }
    </tbody>
</table>